<% include ../partials/header.ejs %>

<div class="container">
    <div class="row">
        <div class="col-sm-4">
            <h2>Courts</h2>
            Local courts are listed here<br><br>
            You can create new courts or play games at currently listed ones
            <div id="message" style="display:none"><h2>Court successfully created</h2></div><br>

        </div>
        <br><br><br>
        <div class="col-sm-6">
            <% if(rs.length > 0) { %>
            <table class="table table-bordered">
                <thead>
                <tr>
                    <th>Court Name</th>
                    <th>City</th>
                    <th>State</th>
                </tr>
                </thead>
                <% for (var i=0; rs.length > i; i++) { %>
                <tbody>
                <tr>
                    <td><a href="/court/?CourtID=<%= rs[i].CourtID %>"><%= rs[i].CourtName %></a></td>
                    <td><%= rs[i].City %></td>
                    <td><%= rs[i].State %></td>
                </tr>
                </tbody>
                <% } %>
            </table>

            <% } else { %>
            No courts exist.
            <% } %>
            <a href="/court/create" type="button" class="btn btn-primary">Add Court</a>
        </div>
    </div>
</div>

<% include ../partials/footer.ejs %>